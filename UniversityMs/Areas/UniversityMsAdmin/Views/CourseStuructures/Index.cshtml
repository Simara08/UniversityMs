@model IEnumerable<UniversityMs.Models.CourseSturucture>

@{
    ViewData["Title"] = "Index";
}

<div class="box-body" style="margin:80px;">
    <a type="submit" asp-action="Create" class="btn btn-primary" style="margin-bottom:10px;">+Add Course Structure</a>
    <div class="table-responsive mb-0">
        <table class="table no-border" style="color: rgb(165, 63, 255);">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.LectureNo)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.IsDaleted)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Attendance)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Course)
                    </th>
                    <th style="min-width: 170px">
                        <span>Setttings</span>
                    </th>
                    <th style="min-width: 120px"></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.LectureNo)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.IsDaleted)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Attendance)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Course.Id)
                        </td>
                        <td>
                            <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                            <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
        </div>
        </div>
